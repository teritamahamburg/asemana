query ($childId: ID!) {
    child(childId: $childId) {
        id
        name
        room {
            number
        }
        checkedAt

        item {
            id
            name
            room {
                number
            }
            checkedAt
        }
    }
}
